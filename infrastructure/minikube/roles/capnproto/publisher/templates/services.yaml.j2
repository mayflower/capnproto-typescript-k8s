---
apiVersion: v1
kind: Service
metadata:
  name: message-publisher-service
  namespace: "{{ namespace.capnproto }}"

spec:
  selector:
    app: message-publisher
  type: ClusterIP
  ports:
    - port: 3001
      name: http
      protocol: TCP